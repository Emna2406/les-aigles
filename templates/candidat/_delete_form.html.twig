<form method="post" action="{{ path('app_candidat_delete', {'idCand': candidat.idCand}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidat.idCand) }}">
   <style>
		.my-button {
			background-color: <?php echo $color; ?>
			;
			color: blue;
			border: none;
			padding: 10px 20px;
			border-radius: 5px;
			cursor: pointer;
		}
	</style>

	<button class="btn btn-lg btn-primary m-2" >Delete</button>
</form>
