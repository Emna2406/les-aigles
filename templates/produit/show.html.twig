{% extends 'base.html.twig' %}

{% block title %}Produit
{% endblock %}

{% block body %}
	<h1>Produit</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ produit.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ produit.nom }}</td>
			</tr>
			<tr>
				<th>Stock</th>
				<td>{{ produit.stock }}</td>
			</tr>
		</tbody>
	</table>
	{{ include('produit/_delete_form.html.twig') }}

	<div>
		<style>
			.my-button {
				background-color: <?php echo $color; ?>
				;
				color: blue;
				border: none;
				padding: 10px 20px;
				border-radius: 5px;
				cursor: pointer;
			}
		</style>

		<a class="my-button" style="float: right;" href="{{ path('app_produit_index') }}">back to list</a>
	</div>

	{# <a href="{{ path('app_produit_edit', {'id': produit.id}) }}">edit</a> #}


{% endblock %}
