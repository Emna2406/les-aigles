{% extends 'base.html.twig' %}

{% block title %}Edit Partenaire
{% endblock %}

{% block body %}
	<h1>Edit Partenaire</h1>

	{{ include('partenaire/_form.html.twig', {'button_label': 'Update'}) }}
	<style>
		.my-button {
			background-color: <?php echo $color; ?>
			;
			color: blue;
			border: none;
			padding: 10px 20px;
			border-radius: 5px;
			cursor: pointer;
		}
	</style>

	<a class="btn btn-lg btn-primary m-2" href="{{ path('app_produit_index') }}">Modifier</a>
	<div>
		<style>
			.my-button {
				background-color: <?php echo $color; ?>
				;
				color: blue;
				border: none;
				padding: 10px 20px;
				border-radius: 5px;
				cursor: pointer;
			}
		</style>

		<a class="my-button" style="float: right;" href="{{ path('app_produit_index') }}">back to list</a>
	</div>

	{# {{ include('partenaire/_delete_form.html.twig') }} #}
{% endblock %}
